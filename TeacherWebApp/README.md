# Система управления преподавателями

Веб-приложение для управления информацией о преподавателях университета.

## 🎯 Описание

Полнофункциональное веб-приложение, которое позволяет:

- ✅ Просматривать список всех преподавателей
- ✅ Искать преподавателей по имени и email
- ✅ Фильтровать по кафедрам
- ✅ Добавлять новых преподавателей
- ✅ Редактировать информацию о преподавателях
- ✅ Удалять записи о преподавателях
- ✅ Просматривать подробную информацию
- ✅ Смотреть статистику по кафедрам и должностям

## 🛠 Технологический стек

- **Фронтенд:** HTML5, CSS3, JavaScript (ванильный)
- **Бэкенд:** Python Flask
- **База данных:** SQLite
- **API:** RESTful JSON API

## 📋 Структура проекта

```
TeacherWebApp/
├── app.py                 # Главное Flask приложение
├── requirements.txt       # Зависимости Python
├── README.md             # Этот файл
├── teachers.db           # База данных SQLite (создается автоматически)
├── static/
│   ├── style.css         # Стили приложения
│   └── script.js         # JavaScript логика
└── templates/
    └── index.html        # HTML главная страница
```

## 🚀 Установка и запуск

### Требования

- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Запуск приложения

```bash
python app.py
```

### Шаг 3: Открыть в браузере

Откройте в браузере:
```
http://localhost:5000
```

## 📊 Структура базы данных

### Таблица: departments (Кафедры)
- `id` - ID кафедры
- `name` - Название кафедры
- `head` - Заведующий кафедрой
- `phone` - Телефон кафедры

### Таблица: teachers (Преподаватели)
- `id` - ID преподавателя
- `first_name` - Имя
- `last_name` - Фамилия
- `patronymic` - Отчество
- `department_id` - ID кафедры
- `position` - Должность
- `email` - Email
- `phone` - Телефон
- `academic_degree` - Ученая степень
- `academic_title` - Ученое звание
- `hire_date` - Дата приема на работу

### Таблица: publications (Публикации)
- `id` - ID публикации
- `teacher_id` - ID преподавателя
- `title` - Название публикации
- `publication_date` - Дата публикации
- `journal` - Журнал/издание

### Таблица: professional_development (Повышение квалификации)
- `id` - ID записи
- `teacher_id` - ID преподавателя
- `course_name` - Название курса
- `completion_date` - Дата завершения
- `certificate` - Номер сертификата

### Таблица: awards (Награды)
- `id` - ID награды
- `teacher_id` - ID преподавателя
- `award_name` - Название награды
- `award_date` - Дата награждения

## 🔌 API Endpoints

### Кафедры
- `GET /api/departments` - Получить список кафедр

### Преподаватели
- `GET /api/teachers` - Получить список преподавателей (с поддержкой фильтрации)
  - Query параметры: `search`, `department`
- `GET /api/teachers/<id>` - Получить информацию о преподавателе
- `POST /api/teachers` - Добавить нового преподавателя
- `PUT /api/teachers/<id>` - Обновить информацию о преподавателе
- `DELETE /api/teachers/<id>` - Удалить преподавателя

### Статистика
- `GET /api/statistics` - Получить статистику

## 📈 Начальные данные

При первом запуске приложение автоматически создает базу данных и загружает 20 преподавателей из 4 кафедр:

- **Информатика** (5 преподавателей)
- **Математика** (5 преподавателей)
- **Физика** (5 преподавателей)
- **Литература** (5 преподавателей)

## 🎨 Интерфейс

### Главная страница
Список всех преподавателей в виде таблицы с возможностью:
- Поиска по имени и email
- Фильтрации по кафедрам
- Просмотра деталей
- Редактирования
- Удаления

### Статистика
- Общее количество преподавателей
- Распределение по кафедрам
- Распределение по должностям

### О системе
Информация о приложении и его возможностях

## 🔐 Безопасность

- Входные данные валидируются на сервере
- Используется CORS для контроля доступа
- SQL-инъекции предотвращаются использованием параметризованных запросов

## 📝 Примеры использования

### Добавить преподавателя

```bash
curl -X POST http://localhost:5000/api/teachers \
  -H "Content-Type: application/json" \
  -d '{
    "first_name": "Иван",
    "last_name": "Иванов",
    "patronymic": "Иванович",
    "department_id": 1,
    "position": "Доцент",
    "email": "ivan@univ.ru",
    "phone": "+7-999-123-45-67",
    "academic_degree": "Кандидат наук",
    "academic_title": "Доцент"
  }'
```

### Получить преподавателей кафедры Информатика

```bash
curl "http://localhost:5000/api/teachers?department=1"
```

### Поиск преподавателя

```bash
curl "http://localhost:5000/api/teachers?search=Иванов"
```

## 🐛 Решение проблем

### Порт 5000 уже занят

Измените порт в `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # Используйте другой порт
```

### Ошибка "ModuleNotFoundError: No module named 'flask'"

Убедитесь, что установили зависимости:
```bash
pip install -r requirements.txt
```

### База данных повреждена

Удалите файл `teachers.db` и перезапустите приложение. База будет пересоздана с начальными данными.

## 📚 Дополнительные возможности

Приложение легко расширяется:

1. **Добавить новые поля** - отредактируйте таблицы в `app.py`
2. **Добавить новые API endpoints** - добавьте маршруты в `app.py`
3. **Изменить дизайн** - отредактируйте `static/style.css`
4. **Добавить функциональность** - расширьте `static/script.js`

## 📄 Лицензия

Учебный проект

## 👨‍💼 Автор

Создано для курсовой работы по дисциплине "Системы управления базами данных"

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что Python установлен: `python --version`
2. Проверьте, что зависимости установлены: `pip list`
3. Посмотрите логи в консоли при запуске приложения
4. Проверьте, что порт 5000 свободен

---

**Приятного использования! 🚀**
